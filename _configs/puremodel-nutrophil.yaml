data:
  csv_file: 'datasets\neutrophil.csv'
  type: 'neutrophil_clinical'

training:
  num_epochs: 50
  batch_size: 64
  learning_rate: 5e-5
  learning_rate_end: 0.0001
  weight_decay: 1e-4
  momentum: 0.9

model:
  image_size: 352
  use_scheduler: False
  num_workers: 8
